@using Microsoft.AspNetCore.Http;
@inject IHttpContextAccessor HttpContextAccessor;
@using System.Text.Json;
@{
    ViewData["Title"] = "Announcement Live Tender";
    Layout = "~/Views/Shared/_UserLayoutdst.cshtml";    
    long LangId = 1;
    try
    {
        LangId = (HttpContextAccessor.HttpContext.Session.Get("LanguageId") == null ? default(long) :
        JsonSerializer.Deserialize<long>(HttpContextAccessor.HttpContext.Session.Get("LanguageId")));
        if (LangId == null)
        {
            NWRWS.Common.SessionWrapper.Set<long>(HttpContextAccessor.HttpContext.Session, "LanguageId", 1);
            LangId = 1;
        }
    }
    catch
    {
    }
}
<link rel="stylesheet" type="text/css" href="@Href("~/Admin/vendors/css/tables/datatable/datatables.min.css?dt="+DateTime.Now.ToString("ddMMyyyyfffttfff"))">
<!-- page-title -->
@* <div class="page-title style-two">
    <div class="pattern-layer" style="background-image: url('@Href("~/ui/images/shape-13.png")');"></div>
    <div class="auto-container">
        <div class="content-box">
            <ul class="bread-crumb clearfix">
                @{
                    var stringData = Functions.GetPublicBreadcum(Url, Context, LangId);
                    if (!string.IsNullOrWhiteSpace(stringData))
                    {
                        @Html.Raw(stringData)
                    }
                }
            </ul>
        </div>
    </div>
</div> *@
<!-- page-title end-->
<div class="ttm-page-title-row ttm-bg ttm-bgimage-yes ttm-bgcolor-darkgrey clearfix">
    <div class="ttm-row-wrapper-bg-layer ttm-bg-layer"></div>
    <div class="container">
        <div class="row align-items-center">
            <div class="col-lg-12">
                <div class="ttm-page-title-row-inner">
                    <div class="breadcrumb-wrapper headersub">
                        @{
                            var stringData = Functions.GetPublicBreadcum(Url, Context, LangId);
                            if (!string.IsNullOrWhiteSpace(stringData))
                            {
                                @Html.Raw(stringData)
                            }
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<section class="ttm-row conatct-section ttm-bgcolor-grey clearfix">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="ttm-bgcolor-white p-40 padding_top35 border-rad_5 margin_top15">
                    <form id="request_qoute_form" class="request_qoute_form wrap-form clearfix" method="post" novalidate="novalidate" action="#">
                        <div class="row">
                          
                            <div class="col-md-3 grcommon">
                                <label>Subject/Title:<span>*</span></label>
                                <span class="text-input">
                                    <input type="text" class="form-control border p-2"
                                           aria-required="true" id="TenderSearchParam" name="TenderSearchParam">
                                </span>
                            </div>
                 


                            <div class="col-lg-12 col-md-12 p-0">
                                <div class="d-sm-flex justify-content-start text-center">
                                    <div class="col-lg-2">
                                        <button class="submit ttm-btn ttm-btn-size-md ttm-btn-shape-rounded ttm-btn-style-fill ttm-btn-color-skincolor w-100 margin_top5" type="button" id="SearchTR">Search</button>
                                    </div>
                                    <div class="col-lg-2">
                                        <button class="submit ttm-btn ttm-btn-size-md ttm-btn-shape-rounded ttm-btn-style-fill ttm-btn-color-skincolor w-100 margin_top5" type="button" id="SearchClear">Reset</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</section>

                                      @*       <div  id="contact-form" class="default-form" novalidate="novalidate">
                                                <div class="row">

                                                    <div class="form-group col-md-6">
                                                        <label>Subject/Title :</label>
                                                        <input type="text" name="text" id="TenderSearchParam" placeholder="" required="" aria-required="true" class="form-control">
                                                    </div>
                                                    <div class="form-group col-md-2 mt-4">
                                                        <button class="theme-btn style-four" id="SearchTR">Search</button>
                                                    </div>
                                                    <div class="form-group col-md-2 mt-4">
                                                        <button class="theme-btn style-four" id="SearchClear">Clear</button>
                                                    </div>
                                                </div>
                                            </div> *@
<section class="ttm-row conatct-section padding_bottom40 ttm-bgcolor-grey clearfix border-rad_5 pb-5" style="padding-top: 0px;">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="ttm-bgcolor-white border-rad_5 grtable">
                                            <div class="table-responsive innertable" id="tableDVArea">
                                                <table id="tableDV" class="table table-striped table-bordered">
                                                    <thead>
                                                        <tr>
                                                            <th>Sr No</th>
                                                            <th>Title</th>
                                                            <th>Pre-Bid Meeting Start Date</th>
                                                            <th>Pre-Bid Meeting End Date</th>
                                                            <th>Last Date of Submission</th>
                                                            <th>More</th>
                                                        </tr>
                                                    </thead>
                                                    <tbody>
                                                    </tbody>
                                                </table>
                                            </div>
            </div>
                  </div>
        </div>
    </div>
</section>

@section Scripts {
    <script src="@Href("~/ui/js/PageDetails/Tender.js?dt="+DateTime.Now.ToString("ddMMyyyyfffttfff"))"></script>
    <script src="@Href("~/Admin/vendors/js/datatable/datatables.min.js?dt="+DateTime.Now.ToString("ddMMyyyyfffttfff"))"></script>
}
